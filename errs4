"${context} cannot contain consecutive commas"        
"${context} cannot end with a comma"       
"${context} cannot start with a comma"       
"${kind} requires a signature list"        
"${kind} requires a string name"        
"${label} must be an identifier"        
"${label} must resolve to an identifier"       
"${label} requires a boolean literal"        
"${label} requires a string literal"        
"abstract callables cannot have a body"       
"abstract clause takes no arguments"        
"abstract is only valid for methods/getters/setters"       
"accessors cannot declare a this parameter"       
"assign requires target and value"        
"await requires an argument"         
"Binding can only have a target and optional initializer"    
"call requires a callee"         
"call-with-this requires a function, thisArg, and optional args"     
"case requires an expression"         
"class-body contains unsupported member statement"        
"class-body must start with (class-body ...)"       
"class cannot declare abstract more than once"      
"class cannot declare typeparams more than once"      
"class cannot have multiple decorators clauses"       
"class cannot have multiple extends clauses"       
"class cannot have multiple implements clauses"       
"class may only contain a single class-body"      
"class requires a name and class-body"       
"constructor cannot be abstract"         
"constructor cannot be async or generator"       
"decorators clause requires at least one expression"      
"default clause requires a type"        
"default pattern cannot wrap rest"        
"default pattern requires a target and value"      
"default requires a value expression"        
"enum-body must start with (enum-body ...)"       
"enum member must be (\"Name\" value?)"       
"enum member name must be a string literal"     
"enum requires a name and enum-body"       
"Expected literal"           
"export-default requires an expression"         
"export named entries must be identifiers or two-element lists"    
"export named entries must contain identifier atoms"      
"export requires a name"         
"export requires at least one spec"       
"export-spec default requires an expression"        
"export-spec entries must be literals, identifiers, or named/default/namespace lists"    
"export-spec must start with (export-spec ...)"       
"export-spec namespace requires a name"        
"Export statement must start with an atom"      
"extends clause requires a type"        
"extends clause requires exactly one expression"       
"Field modifier ${flagNode.type === "atom" ? flagNode.value : "?"}  lacks a value"
"Field modifiers must be atoms"        
"fn param must be a list with a name"    
"fn param must contain a name, optional type, and  optional default value"
"fn param must include a name"       
"fn signature parameters must be lists like ((x string)  (y))"  
"for-await clause requires a binding target and iterable"     
"for-await requires a binding clause"        
"for-await requires a body"         
"for classic accepts at most init, condition, and update"    
"for classic requires a body"        
"for loop requires a kind"        
"for-of clause requires a binding target and iterable"     
"for-of requires a binding clause"        
"for-of requires a body"         
"getter must declare zero parameters"        
"if requires a test and consequent statement"      
"implements clause requires at least one expression"      
"import-all requires an alias and module specifier"      
"import-default requires a name and module specifier"      
"import-named alias entries must contain identifier atoms"      
"import-named entries must be identifiers or two-element lists"     
"import-named requires a list of names and module specifier"    
"import requires an import-spec list"        
"import-spec default requires a name"        
"import-spec entries must be literals, identifiers, or named/default/namespace lists"    
"import-spec must start with (import-spec ...)"       
"import-spec namespace requires a name"        
"import-spec requires a module specifier literal"       
"Import statement must start with an atom"      
"index requires object and index expressions"       
"index-signature only supports optional readonly modifier"       
"index-signature parameter must include name and type"      
"index-signature requires a (name type) pair and a value  type"  
"interface-body must start with (interface-body ...)"       
"Invalid binding"           
"Invalid binding target"          
"Invalid template literal type part"        
"keyword arguments are not supported; use an object literal  instead"  
"lambda cannot be abstract"         
"lambda cannot be overload"         
"lambda cannot declare a this parameter"       
"method requires a name"         
"Module specifier must be a literal"       
"namespace-body must start with (namespace-body ...)"       
"namespace requires a name and namespace-body"       
"new requires a callee"         
"non-null requires an expression"         
"object field key must be a string literal"     
"object fields must be (key value) or (spread object  expr)"  
"object literal key requires a value after ':'"     
"object literal key requires a value; commas cannot appear  between key and
"object-pattern field key must be a string literal"     
"object-pattern fields must be (key target)"       
"object spread must use kind 'object'"       
"object spread requires (spread object expr)"       
"optional key cannot be followed by a comma and  another entry" 
"optional literal field requires a value"       
"overload cannot be abstract"         
"overload is only valid for functions and methods"     
"overload requires a named function"        
"overload signatures cannot be emitted as expressions"      
"overload signatures cannot have a body"       
"parameter properties are not allowed in constructor overloads"     
"parameter properties are only allowed in constructors"      
"parameter property cannot repeat readonly"        
"parameter property cannot specify multiple access modifiers"      
"parameter type annotation requires a type after ':'"     
"Program must be wrapped in (program ...)"      
"Program must start with (program ...)"       
"prop requires an object and a string literal property  name"  
"rest target requires a binding target"       
"return type annotation requires a type after ':'"     
"setter cannot declare a return type"       
"setter must declare exactly one parameter"       
"spread kind must be array, object, or rest"     
"spread requires a kind and an expression"      
"static-block must start with (static-block ...)"       
"switch case head must be an atom"      
"switch cases must be non-empty lists"       
"switch requires a discriminant"         
"t:array requires an element type"        
"t:conditional requires check, extends, true, and false branches"     
"ternary requires test, consequent, and alternate expressions"      
"t:fn requires at least a return type"      
"this parameter cannot be repeated"        
"this parameter must be declared without modifiers"      
"this parameter must be first"        
"this parameter requires a type annotation"       
"throw requires an argument"         
"t:indexed requires an object and an index type"     
"t:infer requires a name"         
"t:keyof requires a target type"        
"t:nullable requires an inner type"        
"t:primitive requires a name"         
"try child must be catch or finally"      
"try child must start with an atom"      
"try requires a body statement"        
"try supports at most one catch"       
"try supports at most one finally"       
"t:typeof requires an expression"         
"t:var requires a name"         
"type-alias expects a single type expression"       
"type-alias requires a name"         
"type-app requires an expression"         
"type-app requires type arguments"         
"type-assert requires an expression and a type"      
"Type constructor must be an atom"       
"Type expressions must be lists"        
"type-function parameters must be type expressions or parameter descriptors"    
"type-function parameters must either all specify types or none  of them do"
"type-function requires at least a return type"      
"type-interface requires a name and interface-body"       
"type-intersection requires at least one member"       
"type-mapped requires a type param and a value type"    
"type-mapped supports at most name remap, modifiers, and via"    
"type-object field key must be an atom"      
"type-object fields must be (key type)"       
"type-param cannot declare multiple variances"        
"type-param contains too many entries"        
"type-param must be a list with a name"     
"type-params list must be a list of identifiers"     
"typeparams list must start with typeparams"       
":type-params requires a list of identifiers"       
"type-ref requires an identifier"         
"type-this does not take arguments"        
"type-union requires at least one member"       
"Unexpected end of input"         
"Unexpected token ${tok.value}"          
"Unknown binding target kind"         
"Unknown field modifier ${flagNode.value}"         
"Unknown import kind ${kind}"         
"Unknown primitive type ${name}"         
"Unknown switch clause ${head.value}"         
"Unknown try child ${head.value}"         
"Unknown type constructor ${head.value}"         
"Unsupported binding target ${(target as any).__brand}"       
"Unsupported binding target in codegen"        
"Unsupported expression kind ${(expr as any).__brand}"       
"Unsupported expression kind ${(serialized as SerializedExpression).kind}"       
"Unsupported for loop kind ${kindNode.value}"        
"Unsupported PhaseB node for cloning: ${(node as PhaseBNodeBase).phaseKind}"     
"Unsupported serialized type node ${(serialized as SerializedTypeNode).kind}"      
"Unsupported statement kind ${(serialized as SerializedStatement).kind}"       
"Unsupported statement kind ${(stmt as any).__brand}"       
"Unsupported token type ${tok.type}"         
"Unsupported type node"          
"Unterminated list"           
"while requires a condition and body"       
