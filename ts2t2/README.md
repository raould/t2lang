# ts2t2

Failed experiment. Proves that (at least some) LLMs cannot just take
this "sexpr" style formatting of TypeScript AST output,
and successfully convert it to t2lang sexpr syntax.
Don't know if they would do any better with just
plain old original .ts.
