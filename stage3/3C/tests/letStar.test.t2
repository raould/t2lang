(program
  ;; let* with multiple bindings and body
  (let* ((x 10)
         (y 20)
         (z (+ x y)))
    ((. console log) z))

  ;; let* with uninitialized binding
  (let* ((a))
    (set! a 42)
    ((. console log) a)))
