(program
  (import (object (:named
    (array
      (object (:name "addValue"))
      (object (:name "VALUE"))
    )
  )) "./A.js")

  (const* ((useA (fn ((n: number)) : number
      (return (addValue n))))
      (RESULT (useA VALUE)))
    ((. console log) "RESULT" RESULT))
)
